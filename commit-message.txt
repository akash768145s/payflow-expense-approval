feat(ui): complete SaaS polish, logo integration, and CI workflow fixes
- Dashboard revamps: Split layout into Recent Expenses (2/3 width) and a custom Category Spending progress chart card (1/3 width) to improve the employee dashboard and eliminate unused space.
- Approvals & Payments: Added sub-tab navigation (Pending Queue vs All Expenses). Dashboard 'View All' now redirects to the All Expenses filter for a smoother workflow.
- Audit Logs: Fixed backend repository queries by including auditLogs in organization and user claim fetches so audit history renders correctly.
- Branding: Integrated the official PayFlow favicon/logo into the login screen, sidebar branding, and updated favicon references across the application.
- Layout Improvements:
  • Sticky top navigation with backdrop blur.
  • Sticky desktop sidebar.
  • Refined spacing and responsive layout.
  • Custom thin scrollbars for a cleaner UI.
- Configuration & CI:
  • Added .gitignore files for root, backend, and frontend.
  • Updated GitHub Actions to execute 'prisma generate' before backend type-checking, fixing CI build failures.
- General Improvements:
  • Improved overall SaaS look and feel.
  • Enhanced navigation consistency.
  • Fixed multiple UI polish issues.
  • Preserved existing functionality while improving user experience.
